# LI SHUAI 作品集 — 界面设计方案

> 设计定位：Modern Editorial / Swiss Grid / Minimal Modern
> 参考方向：CodePen `wodniack/pen/eYBjxaR`（"luge parallax" — 极简、留白、克制的滚动/悬停动效）
> 说明：该 pen 为 JS 实时渲染，静态抓取无法取得源码，以下方案按其"简约现代"特征 + 本次需求推导，而非逐字复制。

---

## 1. 移动优先布局（Mobile-First）

原则：样式从**最小屏（手机）写起**，再用 `min-width` 向上增强；不在大屏写满再往小屏砍。

### 断点系统
| 区间 | 设备 | 关键行为 |
|---|---|---|
| `< 641px` | 手机（base） | 单列堆叠；导航收为汉堡菜单；作品区为横向**可拖拽/可滑动**卡片选择器；遮罩信息**常显**（无 hover） |
| `≥ 641px` | 平板 | 卡片宽度放大；导航展开；遮罩改为**悬停浮现** |
| `≥ 1025px` | 桌面 | 作品区双行布局；Hero 双栏；遮罩悬停浮现；卡片支持 3D 倾斜；页面滚动时两行产生**反向视差错落** |

### 栅格与间距
- 全局 12 列栅格（桌面非 Marquee 区块使用）
- 页面边距 token：`--margin` 手机 20 / 平板 32 / 桌面 56
- 网格间距：`--grid-gap` 手机 16 / 桌面 24
- 区块垂直节奏：手机 72px / 桌面 120px

### 组件响应式要点
- 导航：手机隐藏链接 → 汉堡 + 全屏 overlay；≥641px 显示横向链接
- Hero 标题：`clamp(56px, 9vw, 120px)` 流式缩放，不写死
- 作品区：手机/平板/桌面均为**横向可拖拽轨道**，不再自动循环滚动；桌面悬停触发遮罩浮现 + 视频播放
- 触控设备（`pointer: coarse`）关闭自定义光标与 hover 特效，避免"点了没反应"

---

## 2. 配色方案（Color Tokens）

极简单色为主 + **单一强调色**，符合 minimal modern。

| Token | 值 | 用途 |
|---|---|---|
| `--bg-primary` | `#111111` | 主背景（深色区块、导航） |
| `--bg-secondary` | `#151515` | 卡片底、细节层 |
| `--bg-light` | `#F4F4F1` | 浅色区块（About / Services） |
| `--bg-light-alt` | `#EAEAE7` | 浅色备用 |
| `--text-primary` | `#FFFFFF` | 深底上的文字 |
| `--text-dark` | `#111111` | 浅底上的文字 |
| `--text-muted` | `#777777` | 次要信息、标签 |
| `--accent` | `#CCFF00` | 酸性黄：CTA、激活态、hover 高亮（克制使用） |
| `--line-dark` | `rgba(255,255,255,.15)` | 深底分割线 |
| `--line-light` | `rgba(0,0,0,.15)` | 浅底分割线 |

规则：默认 95% 画面是黑/白/灰，强调色只出现在"可点击 / 当前态 / 关键数据"三处，不铺满。

---

## 3. 字体方案（Typography）

| 角色 | 字体 | 用途 |
|---|---|---|
| 英文 / 正文 | **Fira Sans** | 正文、导航、标签、小字 |
| 英文 / 大标题 / 数字 | **Lexend** | Hero、区块标题、Contact、年份、序号 |
| 中文 / 正文 | **Noto Sans SC** | 段落、作品名、描述 |
| 装饰 / 冲击（备用） | **Climate Crisis** | 极少数强调场景，目前未启用 |

### 字号阶梯（Type Scale）
`12 / 14 / 16 / 18 / 22 / 32 / 56 / 120`（px）

### 排版规范
- 大标题：字重 700，字距 `-0.03 ~ -0.04em`（收紧），行高 0.95–1.05
- 标签 / 英文小字：`uppercase` + 字距 `+0.04~0.08em` + 字重 500–600 + 灰色
- 中文正文：字重 400–500，行高 1.6–1.7，颜色 muted
- 强调数字（年份/序号）：大字号 48–80px，灰色或强调色点缀

---

## 4. 精品作品展示模块规范（Selected Works）

结构：由**两条横向可拖拽/可滑动的卡片轨道**组成，按项目类型细分；用户可自由停留在任意卡片，而非被动等待自动滚动。

- **Row 1 — 汽车 / 科技 / 运动**：迈巴赫 → SCOTT Spark RC → Razer Boomslang
- **Row 2 — 生活食品**：豆腐 → 现挤羊奶 → 预留扩展位（支持后续添加更多食品项目）

> 组件命名参考：**Card Picker / Snap Scroll Strip / Horizontal Carousel**。本站使用 **Card Picker**（卡片选择器）作为统一名称；滚动交互上属于 **Snap Scroll（吸附滚动）**，支持鼠标拖拽与触摸滑动。

### 技术规则
- 容器 `overflow-x: auto` + `scroll-snap-type: x mandatory`，轨道 `width: max-content`
- 每张卡片设置 `scroll-snap-align: center`，滑动/拖拽后自动吸附到卡片中心
- 拖拽时通过 `.dragging` 类临时关闭 snap，松开后浏览器自动吸附
- 拖动惯性：鼠标松开时根据最后速度做衰减滑动，然后 snap 到最近卡片
- 移动端：保留手指滑动 + snap 吸附
- 悬停：当前卡片触发缩放 + 遮罩浮现；轨道不自动滚动，无需暂停

### 视差错落（Parallax Stagger）
页面滚动经过作品区时，两行轨道产生**反向垂直位移**：
- Row 1 随滚动向上偏移（`translateY` 负方向）
- Row 2 随滚动向下偏移（`translateY` 正方向）
- 位移量控制在 ±40px 以内，保持克制，避免破坏阅读
- 与 SVG 示意中"三个矩形以不同速度/方向移动"的逻辑一致

### 卡片状态
- **默认（手机）**：遮罩渐变 + 描述 + `VIEW PROJECT →` 常显；占位大字隐藏
- **默认（桌面）**：仅显示封面 + 底部标题/序号条；占位大字可见
- **Hover（桌面）**：封面平滑缩放 `scale(1.07)` + 底部暗渐变遮罩上浮（`opacity 0→1`）+ 描述与 CTA 浮现 + 占位字淡出 + 自定义光标变 `VIEW` 圆点 + 3D 倾斜
- **点击**：整卡跳转作品详情页

### 交互参数
- 缓动：`cubic-bezier(.16,1,.3,1)`
- 缩放时长：1s；遮罩浮现：0.55s；滚动渐显（IntersectionObserver）0.8s
- 视频：hover 播放 / 离开暂停；`preload="metadata"` 防首屏全量加载

### 响应式
- 手机：卡片宽度约 80vw，横向拖拽/滑动
- 平板：卡片宽度约 55vw
- 桌面：卡片宽度约 38vw，双行上下排列，Row 2 初始左偏移 12vw 形成节奏变化

---

## 5. 内容分区规则

- **精选作品（Selected Works）**：仅放商业项目——汽车/科技/运动 + 食品广告。按项目类型横向拖拽细分。
- **音乐作品（Music）**：独立放在 About 区下方的音乐卡片，包含《怕黑的人》专辑封面、音频、平台链接。**不混入精选作品区**。
- **重点案例（Selected Project）**：当前放迈巴赫视频广告，作为全站的视觉锚点。

---

## 6. 动效原则（Motion）

- 克制：同一屏不超过 1–2 个动效同时发生
- 进入：滚动触发逐块渐显（translateY 40→0）
- 悬停：缩放 + 遮罩，不位移整体布局
- 作品区：由用户主导滚动，提供"选卡"感；页面滚动时两行反向轻微位移形成错落
- 光标：差分式跟随（lerp 0.15），hover 作品时放大为 VIEW/PLAY 圆点
- 禁用：闪烁、弹跳、长时间加载动画
